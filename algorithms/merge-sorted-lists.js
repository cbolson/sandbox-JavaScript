var mergeTwoLists = function (list1, list2) {
  const arr = new Node(null,null);
let prev = arr;

  while(list1 != null && list2!=null){
    
  }
 

  return list1;
};

const a = [1, 2, 4];
const b = [1, 3, 4];

console.log(mergeTwoLists(a, b));
